type TRoutes = {
  home: string;
  login: string;
  register: string;
  contacts: string;
  userInfo: string;
};

export const routes: TRoutes = {
  home: "/",
  login: "/login",
  register: "/register",
  contacts: "/contacts",
  userInfo: "/user-info",
};
